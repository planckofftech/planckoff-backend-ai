[pytest]
testpaths = tests
asyncio_mode = auto
# Timing assertions are meaningless when the rest of the suite is competing for
# CPU. Run them deliberately on a quiet machine: pytest -m perf
addopts = -m "not perf"
markers =
    perf: wall-clock budget checks; deselected by default
filterwarnings =
    ignore::DeprecationWarning

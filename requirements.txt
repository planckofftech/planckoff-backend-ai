fastapi==0.140.8
uvicorn[standard]==0.51.0
python-multipart==0.0.32
pydantic==2.13.4
pydantic-settings==2.14.2
pymupdf==1.28.0
pdfplumber==0.11.10
openai==2.49.0
pillow==12.3.0
openpyxl==3.1.5
supabase==2.31.0
# Cloudflare R2 speaks the S3 API, so the AWS client is what talks to it.
boto3==1.43.77

# Optional. Reads sheets that were flattened to outlines and carry no text at
# all -- without it those sets fail as they always did, and nothing else
# changes. Left out of the default install because onnxruntime and opencv are
# ~150 MB, which is a lot to carry for one set in six.
# rapidocr-onnxruntime==1.4.4

# tests
pytest==9.1.1
pytest-asyncio==1.3.0
httpx==0.28.1


